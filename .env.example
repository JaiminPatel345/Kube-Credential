# Environment Variables Example
# Copy this file to .env and fill in your values

# ===========================================
# LOCAL DEVELOPMENT
# ===========================================

# Shared secret for service-to-service communication
SYNC_SECRET=your-secure-sync-secret-here

# Frontend URL (used for CORS)
FRONTEND_URL=http://localhost:5173

# API URLs for frontend
ISSUANCE_API_URL=http://localhost:3001
VERIFICATION_API_URL=http://localhost:3002

# ===========================================
# AZURE DEPLOYMENT VARIABLES
# (Add these to GitHub Secrets - DO NOT commit actual values)
# ===========================================

# Azure Container Registry name (without .azurecr.io)
# AZURE_CONTAINER_REGISTRY=myregistry

# Azure Container Registry credentials
# AZURE_ACR_USERNAME=myregistry
# AZURE_ACR_PASSWORD=your-acr-password

# Azure Resource Group
# AZURE_RESOURCE_GROUP=kube-credential-rg

# Azure Location (e.g., eastus, westus, westeurope)
# AZURE_LOCATION=eastus

# Azure App Service Name (if using App Service deployment)
# AZURE_APP_SERVICE_NAME=kube-credential-app

# Azure DNS Name Label (for Container Instances)
# AZURE_DNS_NAME_LABEL=kube-credential

# Azure App URL (public URL of your deployed app)
# This URL will be used for BOTH frontend and backend API endpoints
# For Container Instances: https://[DNS_NAME_LABEL].[LOCATION].azurecontainer.io
# For App Service: https://[APP_SERVICE_NAME].azurewebsites.net
# AZURE_APP_URL=https://kube-credential.eastus.azurecontainer.io

# Azure Service Principal credentials (JSON format)
# Get this by running: az ad sp create-for-rbac --sdk-auth
# AZURE_CREDENTIALS={"clientId":"xxx","clientSecret":"xxx","subscriptionId":"xxx","tenantId":"xxx"}

# ===========================================
# NOTES
# ===========================================
# - Storage account not needed: SQLite databases run in container volumes
# - Data persists within container lifecycle (lost if container recreated)
# - For production persistence, consider adding Azure Files or Azure SQL
