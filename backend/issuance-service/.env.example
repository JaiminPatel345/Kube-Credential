# Example environment configuration for the issuance service
# Copy this file to `.env` and adjust values as needed.

# Port the Express server listens on
PORT=3001

# SQLite database location (use :memory: for ephemeral tests)
DATABASE_PATH=data/credentials.db

# Optional worker identifier reported in responses
HOSTNAME=worker-local

# Verification service base URL for sync operations
VERIFICATION_SERVICE_URL=http://localhost:3002

# Optional shared secret for securing sync requests
SYNC_SECRET=change-me

# Runtime environment (development | test | production)
NODE_ENV=development
